package universalelectricity.simulator.dc.macroscopic

import universalelectricity.simulator.dc.macroscopic.component.SeriesComponent

/**
 * Solves the circuit into branches
 * @author Calclavia
 */
class CircuitSolver(source: DCNode, grid: DCGrid)
{
  /**
   * Solves the circuit, returning the series in which this circuit is in
   */
  def solve(): SeriesComponent =
  {
    //Retrieve all possible paths from the source's positive terminal to negative
    return null
  }
}
