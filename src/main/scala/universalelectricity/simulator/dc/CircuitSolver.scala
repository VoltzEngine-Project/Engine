package universalelectricity.simulator.dc

import universalelectricity.simulator.dc.component.SeriesComponent

/**
 * Solves the circuit into branches
 * @author Calclavia
 */
class CircuitSolver(grid: DCGrid)
{
  /**
   * Solves the circuit, returning the series in which this circuit is in
   */
  def solve(): SeriesComponent =
  {
    //Retrieve all possible paths
  }
}
